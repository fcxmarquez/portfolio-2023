import { systemDesign } from "@/styles/vars";
import styled from "styled-components";

export const SectionWrapper = styled.section`
  padding: 2rem ${systemDesign.paddingApplication};
  margin-bottom: 0.375rem;
`;
